package mta.dtos;

import java.util.ArrayList;
import java.util.Objects;
/*
public class CamerasMap {
    public Graph graph;
    public int maxCameras;
    public int minCameras;
    public int vertexAmount;
    public CamerasMap() {
        graph = new Graph();
    }
    public CamerasMap(int maxCameras, int minCameras, int vertexAmount, Graph graph){
        this.maxCameras = maxCameras;
        this.minCameras = minCameras;
        this.vertexAmount = vertexAmount;
        this.graph = graph;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        CamerasMap graph = (CamerasMap) o;
        return vertexAmount == graph.vertexAmount && Objects.equals(edges, graph.edges);
    }

    @Override
    public int hashCode() {
        return Objects.hash(vertexAmount, edges);
    }
}
*/